name: discovery_puzzle
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  logging: ^1.3.0
  get: ^4.7.3
  flame: ^1.21.0
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flame_test: ^1.15.0

flutter:
  uses-material-design: true
  assets:
    - assets/guanyu.png
    - assets/guanyu2.png
